extends layout

block css
	link(rel='stylesheet', type='text/css', href='/stylesheets/header_style.css')
	link(rel='stylesheet', type='text/css', href='/stylesheets/footer_style.css')

block content
  .container
      .row
          .col-xs-10.col-xs-offset-1
              img.contact-image.img-responsive(src='images/pizza-not-found.jpg', alt='Image of a delicious pizza')
  section#error
      .container
          .row
              .col-xs-10.col-xs-offset-1.text-center
                  h1 Hello, pizza lover! We're sorry, we couldn't find this page!
                    button
                      a(href='index.html') Back to homepage
                        .col-xs-10.col6-xs-offset-1.text-center
