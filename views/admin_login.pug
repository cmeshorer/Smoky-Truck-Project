extends admin_layout

block css
	link(rel='stylesheet', type='text/css', href='/stylesheets/admin_login_style.css')

block content
	.container
		.row
			p.col-xs-4.col-xs-offset-4.text-center
				img.img-responsive.img-rounded(src='../images/chef.jpg', alt='Photo of chef')

		.row
			.col-xs-10.col-xs-offset-1.text-center
				h2 Bienvenue sur l'espace administrateur du Smoky Truck !
		.row
			.col-xs-10.col-xs-offset-1.text-center
				form.form-login(method="POST" action="/login")
					// Text input
					.form-group
					label.control-label.text-center(for='login') Identifiant*
					br
					input(name="login" placeholder="Bob" type="text" required)
					br
					// Login input
					.form-group
					label.control-label(for='password') Mot de passe*
					br
					input(name="login" placeholder="Mot de passe" type="password" required)
					br
					br
					// Connexion
					.form-group
						p.text-center.warning
							| * Tous les champs sont obligatoires.
						button.btn.btn-info(type="submit") Connexion


					p.text-center
						| En cas d'oubli de votre identifiant et/ou mot de passe, veuillez 
						a(href='mailto:smokytruck_admin@yopmail.com') contacter
						|  votre administrateur